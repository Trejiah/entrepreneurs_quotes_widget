import 'package:businessmindset/theme/app_color.dart';

/// Thème couleur global assigné au démarrage (`AppBootstrapper`).
/// Ne pas lire avant l’init bootstrap (après `runApp`).
late AppColors appTheme;

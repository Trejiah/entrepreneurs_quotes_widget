<?xml version="1.0" encoding="utf-8"?>
<!--
  Static preview layout shown in the Android widget picker and while the
  Glance widget hasn't rendered its first frame yet. Matches the iOS
  widget preview (dark rounded surface, centered "Tap to configure" prompt,
  share + favorite icons at the bottom corners).
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginTop="12dp"
        android:layout_marginBottom="12dp"
        android:background="@drawable/widget_background"
        android:clipToOutline="true">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:gravity="center"
            android:paddingStart="18dp"
            android:paddingEnd="18dp"
            android:text="@string/widget_tap_to_configure"
            android:textColor="#FFFFF9EE"
            android:textSize="16sp"
            android:textStyle="bold"
            android:maxLines="2"
            android:ellipsize="end" />

        <ImageView
            android:layout_width="22dp"
            android:layout_height="22dp"
            android:layout_gravity="start|bottom"
            android:layout_marginStart="16dp"
            android:layout_marginBottom="14dp"
            android:src="@drawable/ic_widget_share"
            android:tint="#FFFFF9EE"
            android:contentDescription="@null" />

        <ImageView
            android:layout_width="22dp"
            android:layout_height="22dp"
            android:layout_gravity="end|bottom"
            android:layout_marginEnd="16dp"
            android:layout_marginBottom="14dp"
            android:src="@drawable/ic_widget_favorite"
            android:tint="#FFFFF9EE"
            android:contentDescription="@null" />
    </FrameLayout>

</FrameLayout>
